services:
  api:
    ports:
      - "8081:8080"
    environment:
      SPRING_PROFILES_ACTIVE: local
    env_file:
      - ./estime-api/.env.local

  mysql:
    ports:
      - "13306:3306"
    env_file:
      - ./estime-api/.env.local
